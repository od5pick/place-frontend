import defaultBlogImageSystemPrompt from "./blog-image-mode-system-default.txt?raw";

/** GptPromptTemplates.BLOG_IMAGE_MODE_SYSTEM_PROMPT 와 동일 내용(빌드 시 Java와 어긋나면 txt 재생성) */
export const DEFAULT_BLOG_IMAGE_MODE_SYSTEM_PROMPT = defaultBlogImageSystemPrompt;

/**
 * 블로그+이미지(GPT) 시스템 프롬프트에 나오는 영문 이미지 프롬프트 형식 예시와 동일 계열.
 * (GptPromptTemplates BLOG_IMAGE_MODE — STEP 5 예시 문장)
 */
export const DEFAULT_ADMIN_GEMINI_PROMPT = `A quiet Korean cafe interior with few people, calm atmosphere, natural light, minimal design, no text, high resolution`;
